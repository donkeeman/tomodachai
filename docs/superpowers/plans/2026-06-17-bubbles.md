# 고백 말풍선(bubbles) 메커닉 Implementation Plan (Plan 4/N)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 시뮬이 조건 충족 시 `confess_request` 말풍선을 큐에 쌓고(자동 고백 처리 제거), 플레이어가 `POST /api/bubble`로 허락(`allow`)/만류(`stop`)하면 `resolve_confession`이 결과를 이벤트 로그에 push한다. 배고픔(`hungry`) 말풍선도 표시하고 feed로 해소한다. snapshot `bubbles` 스텁을 채운다.

**Architecture:** prototype/game `_maybe_confession_bubble` / `resolve_confession` / `answer_bubble` 규칙을 `src/tomodachai` 모델 위에 재구현.
- 새 모델 `bubble.py`의 `Bubble{kind, char_id, target_id, text}` (프로토타입 dataclass 대응).
- 말풍선 큐 + `confession_count`는 `Simulation`이 소유(relationships/memory/rng와 동거). hungry 생성은 `_update_needs`, 고백 생성은 라이브 경로 `_check_triggered_events_for_pair`.
- `resolve_confession(bubble, approved)`는 `Simulation`. 플레이어 응답 오케스트레이션(`answer_bubble`)·이벤트 기록·hungry 해소는 세션 소유자 `GameState`.
- snapshot은 `{kind, char(이름), target(이름|null), text}`로 직렬화, compat 라우터에 `/bubble` 추가.

**Tech Stack:** Python 3.11, FastAPI, pydantic, pytest.

**기반:** `main`(Plan 1~3 + seed/locations 머지됨). `Mood.adjust()`·이벤트 로그·snapshot 집계기(`map_event`의 `_MAJOR_TYPES`에 `confession_success`/`confession_fail` 이미 포함)·compat 라우터·`relationships.set_lover` 실재.

**Spec:** `docs/superpowers/specs/2026-06-15-babylon-fastapi-connection-design.md` §3.2, §4.6.

**프론트 계약(고정):**
- snapshot `bubbles: {kind, char, target|null, text}[]` (BubbleBox는 `confess_request`만 표시, `hungry`는 표시용).
- `POST /api/bubble {index:int, char:str, answer:"allow"|"stop"}` → `{message}` / `{error}` / `{scene, messages}` (프론트는 `out.error`만 확인 후 재폴링).

**범위 밖 (명시):**
- **방/수면 표시 게이트** (`docs/plan/03` 신규 규칙: 고민 말풍선은 자기 방에서만, 꿈 말풍선은 수면 시): src 백엔드에 **동선/수면 상태 시뮬이 아직 없음**. 따라서 이번엔 프로토타입과 동일하게 조건 충족 시 즉시 큐잉한다. 방 도착/수면 게이트는 동선 시스템 후속 작업으로 분리. (프론트가 캐릭터 위치를 알고 있으니 표시 단계 게이팅은 추후 프론트/동선 연동으로.)
- **고백 LLM 판정**: src에 confession 프롬프트 없음 → 허락 후 수락/거절은 기존 src 방식(`rng < 0.5`) 유지(프로토타입의 "허락 ≠ 보장 성공" 의미 동일). 대사 라인 없이 summary 장면만.
- `bubbles`/`confession_count` 세이브 영속화 안 함(세션 한정 — shop/뉴스/분수대/photos/dishes와 동일). 시뮬 재생성(`reset_world`→`_simulation=None`) 시 자연 초기화.
- rankings (별도 Plan).

---

## 파일 구조

| 파일 | 역할 | 신규/수정 |
|---|---|---|
| `src/tomodachai/bubble.py` | `Bubble` 모델 | 신규 |
| `src/tomodachai/simulation.py` | 말풍선 큐 + `confession_count`, hungry/고백 생성, `resolve_confession` | 수정 |
| `src/tomodachai/game_state.py` | `bubbles` 프로퍼티 + `answer_bubble` + `clear_hungry_bubble` | 수정 |
| `src/tomodachai/api/snapshot.py` | `build_snapshot` bubbles 채움 | 수정 |
| `src/tomodachai/api/snapshot_routes.py` | `POST /bubble` + `/feed` hungry 해소 | 수정 |
| `tests/test_bubble.py` | Bubble 모델 + 생성/resolve 단위 테스트 | 신규 |
| `tests/test_simulation.py` | 고백 트리거 테스트를 버블 검증으로 갱신 | 수정 |
| `tests/test_snapshot.py` | bubbles snapshot + `/bubble`·`/feed`해소 API 테스트 | 수정 |

---

## Task 1: `Bubble` 모델 + Simulation 큐/카운터

**Files:**
- Create: `src/tomodachai/bubble.py`
- Modify: `src/tomodachai/simulation.py`
- Test: `tests/test_bubble.py` (신규)

말풍선 모델과, 시뮬레이션이 소유하는 빈 큐/카운터.

- [ ] **Step 1: Write the failing test** — `tests/test_bubble.py` (신규):

```python
"""고백/배고픔 말풍선 — Bubble 모델 + 시뮬 큐."""

from tomodachai.bubble import Bubble


def test_bubble_defaults():
    b = Bubble(kind="confess_request", char_id=1)
    assert b.kind == "confess_request"
    assert b.char_id == 1
    assert b.target_id is None
    assert b.text == ""


def test_simulation_starts_with_empty_bubbles(make_sim):
    sim = make_sim()
    assert sim.bubbles == []
    assert sim._confession_count == {}
```

`make_sim` 픽스처는 `tests/test_bubble.py` 상단에 정의 (conftest의 mock_llm/sample_personalities 재사용):

```python
import pytest

from tomodachai.character import Character, CharacterState, Customizable, Profile, SpeechHabits
from tomodachai.config import AppConfig, LLMConfig, LocationConfig, SimulationConfig
from tomodachai.simulation import Simulation


def _char(cid: int, name: str, code: str = "outgoing_charmer") -> Character:
    return Character(
        id=cid,
        personality_code=code,
        profile=Profile(name=name, birthday="03-15", blood_type="B", gender="남성"),
        state=CharacterState(),
        customizable=Customizable(speech_habits=SpeechHabits(normal="~")),
    )


@pytest.fixture
def make_sim(mock_llm, sample_personalities):
    def _factory(chars=None):
        cfg = AppConfig(
            llm=LLMConfig(provider="litellm", model="ollama/gemma3", temperature=0.8),
            simulation=SimulationConfig(),
            locations=[LocationConfig(name="공원", capacity=5)],
        )
        chars = chars if chars is not None else [_char(1, "A"), _char(2, "B")]
        return Simulation(config=cfg, characters=chars, llm=mock_llm, personalities=sample_personalities)
    return _factory
```

> 주: `mock_llm`/`sample_personalities`는 `tests/conftest.py` 픽스처(기존 test_simulation.py가 사용). `make_sim`은 인자 없이 호출 시 A·B 2명 기본.

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_bubble.py -v`
Expected: FAIL — `ModuleNotFoundError: tomodachai.bubble` / `sim.bubbles` 없음

- [ ] **Step 3: Write minimal implementation**

`src/tomodachai/bubble.py` (신규):
```python
"""플레이어 응답 대기 말풍선 (고백 허락 요청 / 배고픔 알림).

prototype/game/models.py `Bubble` 대응. 세션 한정(세이브 비대상).
"""

from __future__ import annotations

from pydantic import BaseModel


class Bubble(BaseModel):
    kind: str                      # confess_request / hungry
    char_id: int                   # 말풍선 주인
    target_id: int | None = None   # 고백 대상 (confess_request)
    text: str = ""                 # 표시 문구
```

`src/tomodachai/simulation.py` `Simulation.__init__` 끝(`self._rng = random.Random()` 아래)에 추가:
```python
        # 플레이어 응답 대기 말풍선 큐 + 고백 거절 누적(세션 한정)
        from tomodachai.bubble import Bubble  # noqa: F401  (타입 힌트/런타임 사용)

        self.bubbles: list[Bubble] = []
        self._confession_count: dict[tuple[int, int], int] = {}
```

> `Bubble` import는 모듈 상단으로 올려도 됨(순환 없음: bubble.py는 의존성 없음). 구현자는 상단 import를 선호. `from tomodachai.bubble import Bubble`를 simulation.py 상단 import 블록에 추가하고 `__init__`에는 `self.bubbles: list[Bubble] = []`만 둘 것.

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_bubble.py -v`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add src/tomodachai/bubble.py src/tomodachai/simulation.py tests/test_bubble.py
git commit -m "feat: Bubble 모델 + Simulation 말풍선 큐/confession_count"
```

---

## Task 2: 배고픔 말풍선 생성 (`_update_needs`)

**Files:**
- Modify: `src/tomodachai/simulation.py`
- Test: `tests/test_bubble.py` (추가)

prototype: hunger ≥ 80 이고 그 캐릭터의 hungry 말풍선이 아직 없으면 1개 추가.

- [ ] **Step 1: Write the failing test** — `tests/test_bubble.py`에 추가:

```python
def test_hungry_bubble_added_once(make_sim):
    sim = make_sim()
    sim.characters[0].hunger = 85.0
    sim._update_needs()
    hungry = [b for b in sim.bubbles if b.kind == "hungry" and b.char_id == 1]
    assert len(hungry) == 1
    assert "배고" in hungry[0].text
    # 다시 호출해도 중복 추가 안 함
    sim._update_needs()
    hungry = [b for b in sim.bubbles if b.kind == "hungry" and b.char_id == 1]
    assert len(hungry) == 1


def test_no_hungry_bubble_below_threshold(make_sim):
    sim = make_sim()
    sim.characters[0].hunger = 50.0
    sim._update_needs()
    assert not any(b.kind == "hungry" for b in sim.bubbles)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_bubble.py -k hungry -v`
Expected: FAIL — hungry 말풍선 생성 로직 없음

- [ ] **Step 3: Write minimal implementation**

`src/tomodachai/simulation.py` `_update_needs` 루프 안, hunger 증가/satisfaction 감소 뒤에 추가:
```python
    def _update_needs(self) -> None:
        """Decay satisfaction and increase hunger each tick."""
        for char in self.characters:
            char.hunger = min(100.0, char.hunger + _HUNGER_PER_TICK)
            char.satisfaction = max(0.0, char.satisfaction - _SATISFACTION_DECAY)
            cid = char.id
            if char.hunger >= _HUNGER_BUBBLE_THRESHOLD and not any(
                b.kind == "hungry" and b.char_id == cid for b in self.bubbles
            ):
                self.bubbles.append(
                    Bubble(kind="hungry", char_id=cid, text=f'{char.name}: "배고파요..."')
                )
```

모듈 상단 상수 블록(`_SATISFACTION_DECAY = 1.0` 아래)에:
```python
_HUNGER_BUBBLE_THRESHOLD = 80.0  # 이 이상이면 "배고파요" 말풍선 (prototype 규칙)
```

> `Bubble`은 Task 1에서 상단 import됨. `char.id`는 int(시드/테스트 모두 int).

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_bubble.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/tomodachai/simulation.py tests/test_bubble.py
git commit -m "feat: 배고픔 말풍선 생성 (_update_needs, hunger>=80)"
```

---

## Task 3: 고백 말풍선 생성 (자동 고백 → confess_request 큐잉)

**Files:**
- Modify: `src/tomodachai/simulation.py`
- Test: `tests/test_bubble.py` (추가), `tests/test_simulation.py` (기존 테스트 갱신)

라이브 경로(`_check_triggered_events_for_pair`)의 고백 트리거를 즉시 처리 대신 `confess_request` 말풍선 생성으로 교체. 프로토타입 규칙: A→B `romance ≥ 50 AND friendship ≥ 20`, 이미 연인 아님, `confession_count < 3`, 대기 중 `confess_request` 없음, 50% 확률.

- [ ] **Step 1: Write the failing test** — `tests/test_bubble.py`에 추가:

```python
from tomodachai.relationship import RelationshipStage


def test_confession_creates_bubble_not_event(make_sim, monkeypatch):
    sim = make_sim()
    sim.relationships.update(1, 2, {"friendship": 50, "romance": 75})
    # 트리거 확률 게이트 통과 강제
    monkeypatch.setattr(sim._rng, "random", lambda: 0.0)

    ev = sim._maybe_confession_bubble(1, 2)

    assert any(b.kind == "confess_request" and b.char_id == 1 and b.target_id == 2
               for b in sim.bubbles)
    assert ev is not None and ev["type"] == "bubble"  # 알림 이벤트(피드용), major 아님


def test_only_one_confess_request_pending(make_sim, monkeypatch):
    sim = make_sim()
    sim.relationships.update(1, 2, {"friendship": 50, "romance": 75})
    monkeypatch.setattr(sim._rng, "random", lambda: 0.0)
    sim._maybe_confession_bubble(1, 2)
    # 이미 대기 중이면 두 번째는 생성 안 함
    assert sim._maybe_confession_bubble(1, 2) is None
    assert sum(b.kind == "confess_request" for b in sim.bubbles) == 1


def test_no_confession_bubble_below_thresholds(make_sim, monkeypatch):
    sim = make_sim()
    sim.relationships.update(1, 2, {"friendship": 10, "romance": 75})  # friendship<20
    monkeypatch.setattr(sim._rng, "random", lambda: 0.0)
    assert sim._maybe_confession_bubble(1, 2) is None


def test_no_confession_when_already_lover(make_sim, monkeypatch):
    sim = make_sim()
    sim.relationships.update(1, 2, {"friendship": 50, "romance": 75})
    sim.relationships.set_lover(1, 2)
    monkeypatch.setattr(sim._rng, "random", lambda: 0.0)
    assert sim._maybe_confession_bubble(1, 2) is None
```

`tests/test_simulation.py`의 `test_simulation_confession_trigger`를 **버블 검증으로 갱신** (이벤트 타입 검사 → 버블 큐 검사). 함수 본문 마지막 루프/단언을 교체:

```python
    bubble_appeared = False
    for _ in range(30):
        sim.step()
        if any(b.kind == "confess_request" for b in sim.bubbles):
            bubble_appeared = True
            break

    assert bubble_appeared, "High romance friends should eventually raise a confess_request bubble"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_bubble.py -k confession -v` 및 `pytest tests/test_simulation.py -k confession_trigger -v`
Expected: FAIL — `_maybe_confession_bubble` 없음 / step이 아직 confession 이벤트 생성

- [ ] **Step 3: Write minimal implementation**

`src/tomodachai/simulation.py`에 메서드 추가(`_trigger_confession` 근처):
```python
    def _maybe_confession_bubble(self, a_id, b_id) -> dict | None:
        """조건 충족 시 confess_request 말풍선을 큐에 추가하고 알림 이벤트를 반환.

        prototype 규칙: A→B romance≥50 AND friendship≥20, 이미 연인 아님,
        거절 누적<3, 대기 중 confess_request 없음, 50% 확률.
        """
        if any(b.kind == "confess_request" for b in self.bubbles):
            return None  # 동시 1개만
        if self.relationships.get_slots(a_id).lover == b_id:
            return None  # 이미 연인
        if self._confession_count.get((a_id, b_id), 0) >= 3:
            return None  # 3회 거절 후 단념
        rel = self.relationships.get(a_id, b_id)
        if not (rel.romance >= 50.0 and rel.friendship >= 20.0):
            return None
        if self._rng.random() > 0.5:
            return None

        a_name, b_name = self._name(a_id), self._name(b_id)
        retry = self._confession_count.get((a_id, b_id), 0)
        suffix = " (재도전이에요...)" if retry else ""
        self.bubbles.append(
            Bubble(
                kind="confess_request",
                char_id=a_id,
                target_id=b_id,
                text=f'{a_name}: "{b_name}에게 고백하고 싶어요...{suffix} 해도 될까요?"',
            )
        )
        return {
            "type": "bubble",
            "participants": [a_name, b_name],
            "summary": f"💗 {a_name}이(가) 할 말이 있대요. (말풍선 확인)",
        }
```

`_check_triggered_events_for_pair`에서 고백 블록을 교체 — 기존:
```python
        if (
            rel.romance >= _CONFESSION_ROMANCE_THRESHOLD
            and rel.stage in (RelationshipStage.FRIEND, RelationshipStage.BEST_FRIEND)
            and self._rng.random() < _CONFESSION_CHANCE
        ):
            confession = self._trigger_confession(a_id, b_id)
            if confession:
                events.append(confession)
```
→ 신규:
```python
        bubble_note = self._maybe_confession_bubble(a_id, b_id)
        if bubble_note:
            events.append(bubble_note)
```

> `_trigger_confession`/`_check_triggered_events`(all-pairs, 미사용)는 그대로 둔다(레거시). 라이브 경로만 버블로 전환.

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_bubble.py tests/test_simulation.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/tomodachai/simulation.py tests/test_bubble.py tests/test_simulation.py
git commit -m "feat: 자동 고백 → confess_request 말풍선 큐잉 (prototype 규칙)"
```

---

## Task 4: `resolve_confession(bubble, approved)`

**Files:**
- Modify: `src/tomodachai/simulation.py`
- Test: `tests/test_bubble.py` (추가)

플레이어 응답 처리. 만류(`approved=False`)→단념, 허락(`True`)→수락/거절(`rng<0.5`). 결과 이벤트 dict 반환(`GameState`가 로그에 기록).

- [ ] **Step 1: Write the failing test** — `tests/test_bubble.py`에 추가:

```python
def _confess_bubble(sim, monkeypatch):
    sim.relationships.update(1, 2, {"friendship": 50, "romance": 75})
    monkeypatch.setattr(sim._rng, "random", lambda: 0.0)
    sim._maybe_confession_bubble(1, 2)
    return sim.bubbles[-1]


def test_resolve_confession_giveup_when_not_approved(make_sim, monkeypatch):
    sim = make_sim()
    b = _confess_bubble(sim, monkeypatch)
    ev = sim.resolve_confession(b, approved=False)
    assert ev["type"] == "confession_giveup"
    assert sim._confession_count[(1, 2)] == 3
    assert sim.relationships.get_slots(1).lover != 2  # 연인 안 됨


def test_resolve_confession_success(make_sim, monkeypatch):
    sim = make_sim()
    b = _confess_bubble(sim, monkeypatch)
    monkeypatch.setattr(sim._rng, "random", lambda: 0.0)   # <0.5 → 수락
    ev = sim.resolve_confession(b, approved=True)
    assert ev["type"] == "confession_success"
    assert sim.relationships.get_slots(1).lover == 2
    assert sim.relationships.get_slots(2).lover == 1
    assert sim.relationships.get(1, 2).spark is True
    assert sim._confession_count[(1, 2)] == 0


def test_resolve_confession_fail_increments_count(make_sim, monkeypatch):
    sim = make_sim()
    b = _confess_bubble(sim, monkeypatch)
    monkeypatch.setattr(sim._rng, "random", lambda: 0.99)  # >=0.5 → 거절
    ev = sim.resolve_confession(b, approved=True)
    assert ev["type"] == "confession_fail"
    assert sim._confession_count[(1, 2)] == 1
    assert sim.relationships.get_slots(1).lover != 2
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_bubble.py -k resolve -v`
Expected: FAIL — `resolve_confession` 없음

- [ ] **Step 3: Write minimal implementation**

`src/tomodachai/simulation.py`에 추가:
```python
    def resolve_confession(self, bubble, approved: bool) -> dict:
        """플레이어가 confess_request에 응답한 뒤 처리. 결과 이벤트 dict 반환."""
        a_id, b_id = bubble.char_id, bubble.target_id
        a = self._char_map.get(a_id)
        b = self._char_map.get(b_id)
        a_name, b_name = self._name(a_id), self._name(b_id)

        if not approved:
            # 만류 → 즉시 단념
            self._confession_count[(a_id, b_id)] = 3
            self.relationships.update(a_id, b_id, {"romance": -self._rng.uniform(40, 50)})
            if a:
                a.state.mood.adjust(happiness=-1, stress=1)
            return {
                "type": "confession_giveup",
                "participants": [a_name, b_name],
                "summary": f"{a_name}이(가) {b_name}에 대한 마음을 접었다. (플레이어 만류)",
            }

        success = self._rng.random() < 0.5
        if success:
            self.relationships.set_lover(a_id, b_id)
            rel_ab = self.relationships.get(a_id, b_id)
            rel_ba = self.relationships.get(b_id, a_id)
            rel_ab.spark = True
            rel_ba.spark = True
            self.relationships.update(a_id, b_id, {"romance": 18, "friendship": 10})
            self.relationships.update(b_id, a_id, {"romance": 10, "friendship": 10})
            rel_ab.check_stage_transition(allow_romantic_transition=True)
            rel_ba.check_stage_transition(allow_romantic_transition=True)
            if a:
                a.satisfaction += 15
                a.state.mood.adjust(happiness=3, stress=-2)
            if b:
                b.satisfaction += 10
                b.state.mood.adjust(happiness=2)
            self._confession_count[(a_id, b_id)] = 0
            return {
                "type": "confession_success",
                "participants": [a_name, b_name],
                "summary": f"{a_name}이(가) {b_name}에게 고백하여 연인이 되었다!",
            }

        # 거절
        self.relationships.update(a_id, b_id, {"friendship": -5, "romance": -10})
        self.relationships.update(b_id, a_id, {"friendship": -3})
        count = self._confession_count.get((a_id, b_id), 0) + 1
        self._confession_count[(a_id, b_id)] = count
        if a:
            a.satisfaction = max(0.0, a.satisfaction - 12)
            a.state.mood.adjust(happiness=-3, energy=-1, stress=2)
        if count >= 3:
            self.relationships.update(a_id, b_id, {"romance": -self._rng.uniform(40, 50)})
        return {
            "type": "confession_fail",
            "participants": [a_name, b_name],
            "summary": f"{a_name}이(가) {b_name}에게 고백했지만 거절당했다.",
        }
```

> `confession_success`/`confession_fail`은 snapshot `_MAJOR_TYPES`에 이미 포함 → major 강조. `confession_giveup`은 비-major. scene은 `raw["summary"]`에서 채워짐(map_event 기존 폴백).

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_bubble.py -v`
Expected: PASS (전체)

- [ ] **Step 5: Run full suite**

Run: `pytest tests/ -q`
Expected: all PASS

- [ ] **Step 6: Commit**

```bash
git add src/tomodachai/simulation.py tests/test_bubble.py
git commit -m "feat: resolve_confession (만류→단념 / 허락→수락·거절, set_lover)"
```

---

## Task 5: GameState `bubbles` 프로퍼티 + `answer_bubble` + hungry 해소

**Files:**
- Modify: `src/tomodachai/game_state.py`
- Test: `tests/test_bubble.py` (추가)

세션 오케스트레이션: 프론트 인덱스 검증 → pop → 고백이면 resolve → 이벤트 기록. feed 시 hungry 해소.

- [ ] **Step 1: Write the failing test** — `tests/test_bubble.py`에 추가 (GameState 사용):

```python
from tomodachai.game_state import GameState


def _gs_two():
    cfg = AppConfig(
        llm=LLMConfig(provider="litellm", model="ollama/gemma3", temperature=0.8),
        simulation=SimulationConfig(),
        locations=[LocationConfig(name="공원", capacity=5)],
    )
    gs = GameState(config=cfg)
    gs.add_character(_char(1, "A"))
    gs.add_character(_char(2, "B"))
    return gs


def test_gs_bubbles_property(monkeypatch):
    gs = _gs_two()
    sim = gs.simulation
    sim.relationships.update(1, 2, {"friendship": 50, "romance": 75})
    monkeypatch.setattr(sim._rng, "random", lambda: 0.0)
    sim._maybe_confession_bubble(1, 2)
    assert len(gs.bubbles) == 1


def test_answer_bubble_allow_records_event(monkeypatch):
    gs = _gs_two()
    sim = gs.simulation
    sim.relationships.update(1, 2, {"friendship": 50, "romance": 75})
    monkeypatch.setattr(sim._rng, "random", lambda: 0.0)
    sim._maybe_confession_bubble(1, 2)

    out = gs.answer_bubble(0, "A", allow=True)
    assert "scene" in out
    assert gs.bubbles == []  # 처리되면 제거
    assert any(e["raw"]["type"].startswith("confession") for e in gs.events_since(0))


def test_answer_bubble_name_mismatch(monkeypatch):
    gs = _gs_two()
    sim = gs.simulation
    sim.relationships.update(1, 2, {"friendship": 50, "romance": 75})
    monkeypatch.setattr(sim._rng, "random", lambda: 0.0)
    sim._maybe_confession_bubble(1, 2)
    out = gs.answer_bubble(0, "B", allow=True)  # 0번 주인은 A
    assert "error" in out
    assert len(gs.bubbles) == 1  # 변경 없음


def test_answer_bubble_out_of_range():
    gs = _gs_two()
    out = gs.answer_bubble(5, "A", allow=True)
    assert "error" in out


def test_answer_bubble_hungry_guard():
    gs = _gs_two()
    gs.bubbles.append(Bubble(kind="hungry", char_id=1, text='A: "배고파요..."'))
    out = gs.answer_bubble(0, "A", allow=True)
    assert "error" in out
    assert len(gs.bubbles) == 1  # hungry는 feed로만 해소


def test_clear_hungry_bubble():
    gs = _gs_two()
    gs.bubbles.append(Bubble(kind="hungry", char_id=1, text='A: "배고파요..."'))
    gs.bubbles.append(Bubble(kind="hungry", char_id=2, text='B: "배고파요..."'))
    gs.clear_hungry_bubble(1)
    assert [b.char_id for b in gs.bubbles] == [2]
```

`from tomodachai.bubble import Bubble`는 test_bubble.py 상단에 이미 있음.

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_bubble.py -k "gs_ or answer_ or clear_hungry" -v`
Expected: FAIL — `gs.bubbles` / `answer_bubble` / `clear_hungry_bubble` 없음

- [ ] **Step 3: Write minimal implementation**

`src/tomodachai/game_state.py`에 추가. 프로퍼티(`relationships` 프로퍼티 근처):
```python
    @property
    def bubbles(self) -> list:
        """플레이어 응답 대기 말풍선 큐 (시뮬 소유)."""
        return self.simulation.bubbles
```

메서드(real-time interface 섹션 내, `reset_world` 근처):
```python
    def clear_hungry_bubble(self, char_id) -> None:
        """해당 캐릭터의 배고픔 말풍선 제거 (feed 시 호출)."""
        self.bubbles[:] = [
            b for b in self.bubbles if not (b.kind == "hungry" and b.char_id == char_id)
        ]

    def answer_bubble(self, index: int, char_name: str, allow: bool) -> dict:
        """프론트 말풍선 응답 처리.

        반환: {"error": ...} | {"message": ...} | {"scene": ..., "messages": [...]}.
        """
        bubbles = self.bubbles
        if not (0 <= index < len(bubbles)):
            return {"error": "이미 처리된 말풍선입니다"}
        bubble = bubbles[index]
        owner = self.get_character(bubble.char_id)
        if owner is None or owner.name != char_name:
            return {"error": "말풍선이 갱신되었습니다. 다시 시도해 주세요"}
        if bubble.kind == "hungry":
            return {"error": "배고픔 말풍선은 밥을 주면 사라져요"}
        bubbles.pop(index)
        if bubble.kind != "confess_request":
            return {"message": "말풍선을 확인했습니다"}
        result = self.simulation.resolve_confession(bubble, approved=allow)
        self.record_events([result])
        return {"scene": result["summary"], "messages": []}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_bubble.py -v`
Expected: PASS (전체)

- [ ] **Step 5: Commit**

```bash
git add src/tomodachai/game_state.py tests/test_bubble.py
git commit -m "feat: GameState bubbles 프로퍼티 + answer_bubble + clear_hungry_bubble"
```

---

## Task 6: snapshot `bubbles` 채움

**Files:**
- Modify: `src/tomodachai/api/snapshot.py`
- Test: `tests/test_snapshot.py` (추가)

prototype web_server와 동일 직렬화: `{kind, char(이름), target(이름|null), text}`.

- [ ] **Step 1: Write the failing test** — `tests/test_snapshot.py`에 추가:

```python
def test_snapshot_bubbles_serialized():
    from tomodachai.api.snapshot import build_snapshot
    from tomodachai.bubble import Bubble

    gs = _gs_with_two()
    gs.bubbles.append(
        Bubble(kind="confess_request", char_id=1, target_id=2, text="A: \"고백할래요\"")
    )
    snap = build_snapshot(gs, since=0)
    assert len(snap["bubbles"]) == 1
    b = snap["bubbles"][0]
    assert b["kind"] == "confess_request"
    assert b["char"] == gs.get_character(1).name
    assert b["target"] == gs.get_character(2).name
    assert b["text"] == "A: \"고백할래요\""


def test_snapshot_bubbles_empty_by_default():
    from tomodachai.api.snapshot import build_snapshot

    gs = _gs_with_two()
    assert build_snapshot(gs, since=0)["bubbles"] == []
```

> `_gs_with_two()`는 test_snapshot.py 기존 헬퍼(char_id 1·2). char_id가 시뮬 등록 id와 일치(int)하는지 헬퍼 확인 후 사용. (Plan 1~3 테스트가 `gs.get_character(1)` 사용 → int id 일치.)

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_snapshot.py -k bubbles -v`
Expected: FAIL — bubbles 하드코딩 `[]`

- [ ] **Step 3: Write minimal implementation**

`src/tomodachai/api/snapshot.py`에 직렬화 헬퍼 추가(`map_event` 근처):
```python
def bubble_dict(gs: GameState, bubble) -> dict:
    """Bubble → 프론트 DTO {kind, char, target|null, text}."""
    return {
        "kind": bubble.kind,
        "char": _name_of(gs, bubble.char_id),
        "target": _name_of(gs, bubble.target_id),
        "text": bubble.text,
    }
```

`build_snapshot` 반환 dict의 `"bubbles": [],  # Plan 2(bubble)에서 채움` 를:
```python
        "bubbles": [bubble_dict(gs, b) for b in gs.bubbles],
```

> `_name_of(gs, None)`은 `None` 반환(기존 헬퍼) → target 없을 때 자연히 null.

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_snapshot.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/tomodachai/api/snapshot.py tests/test_snapshot.py
git commit -m "feat: snapshot bubbles 채움 (kind/char/target/text)"
```

---

## Task 7: `POST /api/bubble` 라우트 + `/feed` hungry 해소

**Files:**
- Modify: `src/tomodachai/api/snapshot_routes.py`
- Test: `tests/test_snapshot.py` (추가)

- [ ] **Step 1: Write the failing test** — `tests/test_snapshot.py`에 추가:

```python
def test_api_bubble_allow(client_snap, monkeypatch):
    client, gs = client_snap
    sim = gs.simulation
    sim.relationships.update(1, 2, {"friendship": 50, "romance": 75})
    monkeypatch.setattr(sim._rng, "random", lambda: 0.0)
    sim._maybe_confession_bubble(1, 2)

    resp = client.post("/api/bubble", json={"index": 0, "char": gs.get_character(1).name,
                                            "answer": "allow"})
    assert resp.status_code == 200
    assert "scene" in resp.json()
    snap = client.get("/api/snapshot?since=0").json()
    assert snap["bubbles"] == []
    assert any(e["scene"] for e in snap["events"] if e["major"])  # confession 결과 major


def test_api_bubble_stale_index(client_snap):
    client, _gs = client_snap
    resp = client.post("/api/bubble", json={"index": 9, "char": "A", "answer": "allow"})
    assert resp.status_code == 200
    assert "error" in resp.json()


def test_api_feed_clears_hungry_bubble(client_snap):
    client, gs = client_snap
    from tomodachai.bubble import Bubble
    gs.bubbles.append(Bubble(kind="hungry", char_id=1, text='A: "배고파요..."'))
    # food_id 0 (FOODS[0]) 먹이기
    resp = client.post("/api/feed", json={"char_id": 1, "food_id": 0})
    assert resp.status_code == 200
    assert not any(b.kind == "hungry" and b.char_id == 1 for b in gs.bubbles)
```

> `client_snap` 픽스처는 test_snapshot.py 기존(테스트 client + gs). char_id 1·2 등록.

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_snapshot.py -k "api_bubble or feed_clears" -v`
Expected: FAIL — `/bubble` 없음(404/422), feed가 hungry 미해소

- [ ] **Step 3: Write minimal implementation**

`src/tomodachai/api/snapshot_routes.py`:

`/feed` 라우트의 `gs.record_events([...])` 직전(또는 직후)에 hungry 해소 추가:
```python
    gs.clear_hungry_bubble(body.char_id)
    gs.record_events([{"type": "feed", "participants": [char.name], "summary": msg}])
```

`GiveRequest`/`give_tool` 아래에 추가:
```python
class BubbleRequest(BaseModel):
    index: int
    char: str
    answer: str  # "allow" | "stop"


@compat_router.post("/bubble")
def answer_bubble(body: BubbleRequest):
    gs = _gs()
    return gs.answer_bubble(body.index, body.char, allow=body.answer == "allow")
```

> 프로토타입 계약과 동일하게 오류도 200 + `{"error": ...}` (프론트가 `out.error`로 분기). HTTPException 미사용.

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_snapshot.py -v`
Expected: PASS

- [ ] **Step 5: Run full suite + ruff**

Run: `pytest tests/ -q` 및 `ruff check src/tomodachai/bubble.py src/tomodachai/simulation.py src/tomodachai/game_state.py src/tomodachai/api/snapshot.py src/tomodachai/api/snapshot_routes.py tests/test_bubble.py`
Expected: all PASS, ruff clean (이번 작업 도입/수정 라인 기준)

- [ ] **Step 6: Commit**

```bash
git add src/tomodachai/api/snapshot_routes.py tests/test_snapshot.py
git commit -m "feat: POST /api/bubble + /feed 시 배고픔 말풍선 해소"
```

---

## Self-Review 결과 (작성자 점검)

- **Spec 커버리지(§4.6):** confess_request 큐잉(Task3)·플레이어 허락/만류→resolve_confession(Task4)·결과 이벤트 push(Task5)·snapshot 노출(Task6)·`POST /bubble`(Task7). hungry는 feed로만 해소(Task5·7), 승인 대상 아님(answer_bubble 가드). 자동 고백 제거(라이브 경로 교체, Task3).
- **프론트 계약 정합:** Bubble DTO `{kind, char, target|null, text}`(Task6) ↔ `types.ts Bubble`. `POST /bubble {index, char, answer}` → `{error}`/`{scene,messages}`(Task7) ↔ `api.ts answerBubble`·`BubbleBox.svelte`(confess_request만 표시, `out.error` 분기). 오류 200+error 반환(프로토타입 동일).
- **타입 일관성:** `Bubble.char_id:int`/`target_id:int|None` ↔ 시뮬/슬롯 int id 일치. `resolve_confession(bubble, approved)->dict`(Task4) ↔ `answer_bubble` 호출(Task5) ↔ `record_events`(이벤트 dict shape: type/participants/summary, 기존 map_event 처리) 일치. `set_lover`·`check_stage_transition(allow_romantic_transition=True)`·`Mood.adjust`·`char.satisfaction`(property) 실재.
- **잠복 버그 수정:** 기존 `_trigger_confession` 성공 시 stage만 LOVER로 바꾸고 `set_lover` 슬롯 미설정 → snapshot이 `slots.lover`를 읽어 연인 미표시. 신규 `resolve_confession`은 `set_lover` 호출로 슬롯·stage 동시 정합.
- **죽은 코드 보존:** `_check_triggered_events`(all-pairs, 미호출)·`_trigger_confession`은 레거시로 미변경. 라이브 경로 `_check_triggered_events_for_pair`만 버블로 전환. `_CONFESSION_ROMANCE_THRESHOLD`/`_CONFESSION_CHANCE` 상수는 레거시가 계속 참조(미삭제).
- **세션 한정:** bubbles/`_confession_count`는 Simulation 소유 → save.py 비대상. `reset_world()`가 `_simulation=None`으로 자연 초기화(추가 정리 불필요).
- **테스트 결정성:** 확률 게이트는 `monkeypatch.setattr(sim._rng, "random", ...)`로 고정(0.0=통과·수락, 0.99=거절). 기존 `test_simulation_confession_trigger`는 버블 검증으로 갱신(Task3).
- **범위 밖 명시:** 방/수면 표시 게이트(동선 시스템 의존), 고백 LLM 판정(프롬프트 부재), 세이브 영속화, rankings.
```
